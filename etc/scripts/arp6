packet-generator new {
  name x
  limit 1
  node ip6-input
  size 64-64
  no-recycle
  data {
    ICMP6: ab::2 -> ab::1
    ICMP echo_request
    incrementing 100
  }
}

tr add pg-input 100
set int state pg/stream-0 up
set int ip address pg/stream-0 ab::/64
ip route ab::1/128 via arp pg/stream-0