packet-generator new {
  name x
  limit 1
  node ip6-input
  size 64-64
  no-recycle
  data {
    ICMP6: ab::2 -> ab::1
    ICMP echo_request
    incrementing 100
  }
}

tr add pg-input 100
int create-ethernet
set int state fake-eth0 up
set int ip address fake-eth0 ab::/64
